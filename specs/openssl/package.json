{
  "author": "OpenSSL",
  "dependencies": {
    "@buckpkg/buckit": "*"
  },
  "description": "Sets up the system-installed Openssl tools for use with buck",
  "flat": true,
  "license": "OpenSSL",
  "name": "@buckpkg/openssl",
  "buckit": {
    "required_system_packages": {
      "darwin": ["openssl"]
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/openssl/openssl"
  },
  "version": "0.0.1",
  "scripts": {
    "postinstall": "buckit buckconfig"
  }

}
